.site-subtitle { display: none; }
.custom-logo {
  .site-meta-headline { text-align: center; }

  .brand { background: none; }

  .site-title {
    margin: 10px auto 0;
    font-size: 24px;
    color: $black-deep;
    a { border: none; }
  }


}

.custom-logo-image {
  margin: 0 auto;
  max-width: 250px;
  background: white;
}
